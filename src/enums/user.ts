export enum USER_ROLES {
    ADMIN = 'ADMIN',
    SUPER_ADMIN = 'SUPER_ADMIN',
    USER = 'USER',
}

export enum USER_STATUS {
    ACTIVE = "active",
    BLOCK = "block",
    ARCHIVE = "archive",
}

export enum USER_REPORT {
    REPORT = "report",
    NO_REPORT = "no_report"
}