"use strict";
// import express from "express";
// import { ReportController } from "./report.controller";
// import auth from "../../middlewares/auth";
// import { USER_ROLES } from "../../../enums/user";
// const router = express.Router();
// // User report another user
// router.post("/:id", auth(USER_ROLES.USER), ReportController.createReport);
// // Admin update report status or warning
// router.patch("/action/:id", auth(USER_ROLES.ADMIN , USER_ROLES.SUPER_ADMIN), ReportController.adminAction);
// // Admin get all reports
// router.get("/all", auth(USER_ROLES.ADMIN, USER_ROLES.SUPER_ADMIN), ReportController.getAllReports);
// router.get("/:id", auth(USER_ROLES.ADMIN, USER_ROLES.SUPER_ADMIN), ReportController.getSingleReports);
// export const ReportRoutes = router;
